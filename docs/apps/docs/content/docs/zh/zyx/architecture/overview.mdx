---
title: 架构概述
description: ZYX 的分层结构、核心对象与主执行路径。
category: 概述
categoryOrder: 1
order: 1
project: zyx
projectLabel: ZYX
projectDescription: High-performance embedded graph database engine
projectOrder: 1
---
# 架构概述

ZYX 是可嵌入式图数据库引擎，核心是单进程内库调用模型，不依赖独立服务进程。

## 分层结构

```mermaid
graph TB
    subgraph "应用层"
        CLI[CLI REPL]
        BM[Benchmark]
        APP[宿主应用]
    end

    subgraph "API 层"
        CPP["C++ API (zyx.hpp)"]
        CA["C API (zyx_c_api.h)"]
        WASM["WASM Playground"]
    end

    subgraph "查询层"
        Parser["ANTLR4 Cypher 解析器"]
        Planner["逻辑计划器 + 优化器"]
        Executor["物理执行器"]
    end

    subgraph "存储层"
        DM[DataManager]
        PM[PersistenceManager]
        FS[FileStorage]
        WAL[WALManager]
        PB[PageBufferPool]
    end

    subgraph "核心层"
        DB[Database]
        TM[TransactionManager]
        TX[Transaction]
    end

    CLI --> CPP
    BM --> CPP
    APP --> CPP
    CA --> DB
    WASM --> CA

    CPP --> DB
    DB --> TM
    DB --> TX

    TX --> DM
    TX --> WAL

    Parser --> Planner --> Executor
    Executor --> DM

    DM --> PM
    PM --> FS
    DM --> PB
```

**各层职责：**

- **应用层** — CLI 工具、benchmark 套件以及嵌入 ZYX 的宿主应用。
- **API 层** — C++ API（`zyx.hpp`）、C API（`zyx_c_api.h`）及 WASM 绑定。
- **查询层** — Cypher 文本经解析、计划、优化后，作为物理算子流水线执行。
- **存储层** — `DataManager` 统一管理实体读写，`PersistenceManager` 跟踪脏数据，`FileStorage` 管理段式文件，`WALManager` 处理预写日志，`PageBufferPool` 提供 LRU 段缓存。
- **核心层** — `Database` 为顶层入口；`TransactionManager` 实现单写多读并发；`Transaction` 为外部句柄。

## 关键对象

| 对象 | 职责 | 源码位置 |
|------|------|----------|
| `graph::Database` | 生命周期管理，QueryEngine / ThreadPool / WAL 惰性初始化 | `include/graph/core/Database.hpp` |
| `graph::query::QueryEngine` | Cypher 解析、计划构建与执行 | `include/graph/query/api/QueryEngine.hpp` |
| `graph::storage::FileStorage` | 段式文件布局，flush/save 协调 | `include/graph/storage/FileStorage.hpp` |
| `graph::storage::DataManager` | Node / Edge / Property / Blob / Index / State 统一 I/O 入口 | `include/graph/storage/data/DataManager.hpp` |
| `TransactionManager` | 单写锁、快照管理、commit/rollback 协调 | `include/graph/core/TransactionManager.hpp` |

## 主执行路径

### 查询路径

```mermaid
sequenceDiagram
    participant App
    participant DB as Database
    participant QE as QueryEngine
    participant DM as DataManager

    App->>DB: beginTransaction()
    DB->>App: Transaction（已获取写锁）
    App->>QE: execute("CREATE (n:User {name: 'Alice'})")
    QE->>QE: 解析 → 逻辑计划 → 优化 → 物理计划
    QE->>DM: 执行算子
    DM->>DM: 分配 ID、写入实体、标记脏
    App->>App: tx.commit()
    Note over App,DM: 见下方写入路径
```

### 写入路径（提交）

```mermaid
sequenceDiagram
    participant TX as Transaction
    participant WAL as WALManager
    participant PM as PersistenceManager
    participant SW as StorageWriter
    participant FS as FileStorage

    TX->>WAL: 写入 WAL_TXN_COMMIT + sync
    TX->>PM: createSnapshot() 捕获所有脏注册表
    PM-->>TX: FlushSnapshot（nodes, edges, props, blobs, indexes, states）
    TX->>SW: writeSnapshot(snapshot)
    SW->>FS: 将各实体类型持久化到对应段链
    SW->>FS: 更新 FileHeader（max IDs）
    TX->>WAL: 若达到阈值则 checkpoint()
    TX->>PM: commitSnapshot() — 交换双缓冲、清空 flushing 集
    TX->>TX: 释放写锁，发布新 CommittedSnapshot
```

### 读取路径

```mermaid
sequenceDiagram
    participant App
    participant TX as Transaction（只读）
    participant DM as DataManager
    participant PM as PersistenceManager
    participant PB as PageBufferPool
    participant IO as StorageIO

    App->>TX: beginReadOnlyTransaction()
    TX->>TX: 获取共享锁，捕获 CommittedSnapshot
    App->>DM: getNode(id)
    DM->>PM: 检查 flushing 脏集
    alt 脏数据命中
        PM-->>DM: 从脏注册表返回
    else 干净数据
        DM->>PB: getPage(segmentOffset)
        alt 缓存命中
            PB-->>DM: 缓存页
        else 缓存未命中
            PB->>IO: pread（段数据）
            IO-->>PB: 原始字节
            PB-->>DM: 页数据
        end
    end
    DM-->>App: Node
```

## 设计原则

1. **嵌入式优先** — ZYX 是库而非服务器，直接链接到进程内。
2. **单写多读** — `std::shared_mutex` 提供排他写权限，同时允许多个并发读。
3. **段式存储** — 固定大小（128 KB）段，每实体类型独立链表。
4. **惰性初始化** — `QueryEngine`、`ThreadPool`、`WAL` 仅在首次访问时创建，保持启动快速。
5. **双缓冲脏跟踪** — `PersistenceManager` 维护 active/flushing 两组脏映射，使读取可在 I/O 期间继续。

## 源码定位

| 组件 | 头文件 | 实现 |
|------|--------|------|
| Database | `include/graph/core/Database.hpp` | `src/core/Database.cpp` |
| QueryEngine | `include/graph/query/api/QueryEngine.hpp` | `src/query/` |
| FileStorage | `include/graph/storage/FileStorage.hpp` | `src/storage/` |
| TransactionManager | `include/graph/core/TransactionManager.hpp` | `src/core/TransactionManager.cpp` |
| WAL | `include/graph/storage/wal/WALManager.hpp` | `src/storage/wal/` |
