---
title: 高级查询
description: 使用 WITH/UNION/UNWIND 组装查询管线，结合过程/子查询与 LOAD CSV。
category: 高级主题
order: 6
project: zyx
projectLabel: ZYX
projectDescription: High-performance embedded graph database engine
projectOrder: 1
---
# 高级查询

## 1) 用 WITH 切分管线

```cypher
MATCH (p:Person)
WITH p, p.age AS age
WHERE age >= 30
RETURN p.name, age
ORDER BY age DESC;
```

## 2) UNION / UNION ALL

```cypher
MATCH (n:Person) RETURN n.name AS name
UNION ALL
MATCH (n:Company) RETURN n.name AS name;
```

## 3) UNWIND 展开行

```cypher
UNWIND [1, 2, 3] AS x
RETURN x, x * 10;
```

## 4) CALL 子查询与事务分批

```cypher
MATCH (n:Input)
CALL {
  WITH n
  CREATE (:Output {v: n.val})
} IN TRANSACTIONS OF 1000 ROWS
RETURN n.val;
```

## 5) LOAD CSV

带表头：

```cypher
LOAD CSV WITH HEADERS FROM 'file:///tmp/users.csv' AS row
RETURN row.name, row.age;
```

自定义分隔符：

```cypher
LOAD CSV WITH HEADERS FROM 'file:///tmp/users.tsv' AS row FIELDTERMINATOR '\t'
RETURN row.name;
```

## 6) 内置过程能力面

| 领域 | 过程 |
|---|---|
| 配置与统计 | `dbms.listConfig`, `dbms.getConfig`, `dbms.setConfig`, `dbms.showStats`, `dbms.resetStats` |
| 向量 | `db.index.vector.queryNodes`, `db.index.vector.train` |
| 轻量算法 | `algo.shortestPath` |
| GDS 投影 | `gds.graph.project`, `gds.graph.drop` |
| GDS 算法 | `gds.pageRank.stream`, `gds.wcc.stream`, `gds.betweenness.stream`, `gds.closeness.stream`, `gds.shortestPath.dijkstra.stream` |

## 7) EXPLAIN / PROFILE

```cypher
EXPLAIN MATCH (n:Person {name: 'Alice'}) RETURN n;
PROFILE MATCH (n:Person {name: 'Alice'}) RETURN n;
```

```mermaid
flowchart LR
  A[编写查询] --> B[EXPLAIN]
  B --> C[检查计划结构]
  C --> D[PROFILE]
  D --> E[定位热点]
  E --> F[调整查询或索引]
```
