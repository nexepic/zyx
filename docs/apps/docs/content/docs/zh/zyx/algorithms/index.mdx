---
title: 算法详解
description: ZYX 中与查询、索引、存储、向量检索相关的核心算法入口。
category: 概述
categoryOrder: 1
order: 1
project: zyx
projectLabel: ZYX
projectDescription: High-performance embedded graph database engine
projectOrder: 1
---
# 算法详解

本章节重点回答三个问题：

1. 这个算法解决什么问题
2. 在代码中由哪些组件实现
3. 实际运行时怎样与其它层协同

## 推荐阅读

- 全局总览： [算法总览](overview)
- 查询相关： [查询优化](query-optimization)、[关系遍历](relationship-traversal)
- 索引相关： [B+Tree 索引](btree-indexing)、[标签索引](label-index)、[属性索引](property-index)
- 存储相关： [段分配](segment-allocation)、[位图索引](bitmap-indexing)、[压缩](compression)、[缓存淘汰](cache-eviction)
- 向量相关： [向量度量](vector-metrics)、[PQ](product-quantization)、[KMeans](kmeans)、[DiskANN](diskann)
- 事务与恢复： [WAL 恢复](wal-recovery)、[状态链与并发](state-chain-optimistic-locking)

## 建议阅读顺序

1. 先看 [算法总览](overview)，建立系统级心智模型
2. 再看查询与索引（`query-optimization` + `btree-indexing` / `label-index` / `property-index`）
3. 再看存储与恢复（`segment-allocation` / `bitmap-indexing` / `compression` / `wal-recovery`）
4. 最后看向量算法（`vector-metrics` / `product-quantization` / `kmeans` / `diskann`）
