{
  "name": "zyx-docs-temp",
  "version": "0.1.2",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "cd apps/docs && bun run dev",
    "build": "cd apps/docs && bun run build",
    "start": "cd apps/docs && bun run start",
    "clean": "rm -rf apps/docs/.next apps/docs/.swc apps/docs/.content-collections apps/docs/tsconfig.tsbuildinfo",
    "lint": "cd apps/docs && bun run lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\""
  },
  "devDependencies": {
    "prettier": "^3.2.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "bun@1.0.0"
}
