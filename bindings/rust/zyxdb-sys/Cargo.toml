[package]
name = "zyxdb-sys"
version = "0.1.29"
edition = "2021"
description = "Raw Driver ABI bindings for the ZYX embedded graph database"
license = "Apache-2.0"
repository = "https://github.com/nexepic/zyx"
homepage = "https://try-zyx.github.io/zyx"
documentation = "https://try-zyx.github.io/zyx/en/docs/zyx/api"
readme = "README.md"
keywords = ["graph", "database", "cypher", "ffi"]
categories = ["database", "external-ffi-bindings"]
links = "zyx"
build = "build.rs"

[features]
default = ["bundled"]
bundled = ["dep:zyx-src"]
system = []

[lib]
name = "zyxdb_sys"
path = "src/lib.rs"

[dependencies]

[build-dependencies]
zyx-src = { path = "../../..", version = "0.1.29", optional = true }
