project('simsimd', 'c', 'cpp', version : '6.5.10')

# SimSIMD is a header-only library.
# We expose the 'include' directory so consumers can #include <simsimd/simsimd.h>
simsimd_dep = declare_dependency(
	include_directories : include_directories('include')
)